[cosmic-ray]
module-path = [
  "src/InkGen/extraction_truth.py",
]
timeout = 20.0
excluded-modules = []
test-command = "python -m pytest -x tests/test_extraction_truth.py tests/test_grammar_truth.py tests/test_pdf_generator.py"

[cosmic-ray.distributor]
name = "local"

[cosmic-ray.filters.operators-filter]
exclude-operators = [
  # The extraction module uses postponed type annotations. These mutants alter
  # `X | None` annotations, not runtime behavior in this slice.
  "core/ReplaceBinaryOperator_BitOr_.*",
]
